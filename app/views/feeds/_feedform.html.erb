<%= form_with(model: @feed,local: true, url: new_or_edit ) do |form| %>

<%= form.label :title %>
<%= form.text_field :title %>

<%= form.label :content %>
<%= form.text_field :content %>

<%= form.submit "投稿"%>
<% end %>
